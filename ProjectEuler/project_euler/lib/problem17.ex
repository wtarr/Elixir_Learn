defmodule Problem17 do

  # def countLettersOfNumberAsWord(number) do
  #
  # end
  #
  # def loadFile(filename) do
  #  {_result, device} = File.open(filename, [:read, :utf8])
  #  preBuildListof1to100numberWordCountCombo(device, [], 1, &countLettersOfNumberAsWord/1)
  # end
  #
  # def preBuildListof1to100numberWordCountCombo(file, count, lines, countFunc) do
  #   line = IO.read(file, :line)
  #   case line do
  #     :eof ->
  #       File.close(file)
  #       lines
  #     _ ->
  #       l = lines ++ [{count, countFunc line}]
  #   end
  # end


end
